apiVersion: v1
kind: Service
metadata:
  name: {{ include "setup-platform-sonarqube.fullname" . }}
  namespace: {{ include "setup-platform-sonarqube.namespace" . }}
  labels:
    {{- include "setup-platform-sonarqube.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: sonarqube
  selector:
    {{- include "setup-platform-sonarqube.selectorLabels" . | nindent 4 }}